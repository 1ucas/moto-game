# TODO

## Performance

- [ ] Add client-side debounce to collision detection to prevent `collect-pickup` and `complete-delivery` event spam (currently emits every animation frame while in pickup zone)
